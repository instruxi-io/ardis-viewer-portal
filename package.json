{
  "name": "ardis-viewer-portal",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "selfcheck": "node scripts/envelope-selfcheck.mjs",
    "check": "node scripts/envelope-selfcheck.mjs && node scripts/no-html-injection.mjs"
  },
  "dependencies": {
    "ethers": "^6.13.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
